# Render blueprint for the API. The web app deploys to Vercel with root directory `web`.
services:
  - type: web
    name: docchat-api-asadullah
    runtime: node
    rootDir: api
    plan: free
    buildCommand: npm ci && npm run build && npm run warm-model
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "22"
      - key: SEED_ON_BOOT
        value: "true"
      # Groq leads: Gemini's free tier has a small daily allowance. Either can be primary.
      - key: LLM_PROVIDER
        value: "groq"
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: WEB_ORIGIN
        sync: false
      - key: GEMINI_MODEL
        value: "gemini-3.5-flash-lite"
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
